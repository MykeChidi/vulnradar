<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 20px;
            min-height: 100vh;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            background: white;
            border-radius: 10px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            overflow: hidden;
        }
        
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 40px 30px;
            text-align: center;
        }
        
        .header h1 {
            font-size: 2.5em;
            margin-bottom: 10px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
        }
        
        .header p {
            font-size: 1.1em;
            opacity: 0.9;
        }
        
        .content {
            padding: 40px 30px;
        }
        
        .info-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-bottom: 40px;
            padding-bottom: 30px;
            border-bottom: 2px solid #eee;
        }
        
        .info-box {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 8px;
            border-left: 4px solid #667eea;
        }
        
        .info-box label {
            display: block;
            font-weight: 600;
            color: #333;
            font-size: 0.9em;
            text-transform: uppercase;
            margin-bottom: 8px;
            letter-spacing: 0.5px;
        }
        
        .info-box value {
            display: block;
            font-size: 1.1em;
            color: #667eea;
            word-break: break-all;
        }
        
        .section {
            margin-bottom: 50px;
        }
        
        .section h2 {
            color: #667eea;
            font-size: 1.8em;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 3px solid #667eea;
        }
        
        .summary {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 15px;
            margin-bottom: 30px;
        }
        
        .summary-card {
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            color: white;
            padding: 20px;
            border-radius: 8px;
            text-align: center;
            box-shadow: 0 4px 15px rgba(245, 87, 108, 0.3);
        }
        
        .summary-card.high {
            background: linear-gradient(135deg, #f5576c 0%, #d92e3a 100%);
        }
        
        .summary-card.medium {
            background: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
        }
        
        .summary-card.low {
            background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
        }
        
        .summary-card .number {
            font-size: 2.5em;
            font-weight: bold;
            display: block;
        }
        
        .summary-card .label {
            font-size: 0.9em;
            opacity: 0.9;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            overflow: hidden;
        }
        
        thead {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }
        
        th {
            padding: 15px;
            text-align: left;
            font-weight: 600;
            text-transform: uppercase;
            font-size: 0.85em;
            letter-spacing: 0.5px;
        }
        
        td {
            padding: 12px 15px;
            border-bottom: 1px solid #eee;
        }
        
        tbody tr:hover {
            background: #f8f9fa;
        }
        
        tbody tr:last-child td {
            border-bottom: none;
        }
        
        .severity {
            display: inline-block;
            padding: 5px 12px;
            border-radius: 20px;
            font-weight: 600;
            font-size: 0.85em;
            text-transform: uppercase;
        }
        
        .severity.critical {
            background: #f5576c;
            color: white;
        }
        
        .severity.high {
            background: #fa709a;
            color: white;
        }
        
        .severity.medium {
            background: #fee140;
            color: #333;
        }
        
        .severity.low {
            background: #4facfe;
            color: white;
        }
        
        .severity.info {
            background: #00f2fe;
            color: #333;
        }
        
        .no-data {
            text-align: center;
            padding: 40px;
            color: #999;
            font-style: italic;
        }
        
        .endpoint-badge {
            background: #e7e7ff;
            color: #667eea;
            padding: 3px 8px;
            border-radius: 4px;
            font-family: 'Courier New', monospace;
            font-size: 0.9em;
        }
        
        .tech-tag {
            display: inline-block;
            background: #f0f0f0;
            color: #333;
            padding: 5px 10px;
            border-radius: 4px;
            margin: 3px;
            font-size: 0.85em;
            border: 1px solid #ddd;
        }
        
        .footer {
            background: #f8f9fa;
            padding: 20px 30px;
            text-align: center;
            color: #666;
            font-size: 0.9em;
            border-top: 1px solid #eee;
        }
        
        .footer p {
            margin: 5px 0;
        }
        
        @media (max-width: 768px) {
            .info-grid {
                grid-template-columns: 1fr;
            }
            
            .header h1 {
                font-size: 1.8em;
            }
            
            table {
                font-size: 0.9em;
            }
            
            th, td {
                padding: 10px;
            }
        }
        
        @media print {
            body {
                background: white;
                padding: 0;
            }
            
            .container {
                box-shadow: none;
                border-radius: 0;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <h1>🔒 Vulnerability Scan Report</h1>
            <p>{{ title }}</p>
        </div>
        
        <!-- Main Content -->
        <div class="content">
            <!-- Scan Information -->
            <div class="info-grid">
                <div class="info-box">
                    <label>Target URL</label>
                    <value>{{ target }}</value>
                </div>
                <div class="info-box">
                    <label>Scan Date & Time</label>
                    <value>{{ scan_time }}</value>
                </div>
                <div class="info-box">
                    <label>Total Endpoints</label>
                    <value>{{ endpoints|length }}</value>
                </div>
                <div class="info-box">
                    <label>Technologies Detected</label>
                    <value>{{ technologies|length }}</value>
                </div>
            </div>
            
            <!-- Vulnerability Summary -->
            {% if vulnerabilities %}
            <div class="section">
                <h2>📊 Vulnerability Summary</h2>
                
                {% set critical = vulnerabilities|selectattr('severity', 'equalto', 'Critical')|list %}
                {% set high = vulnerabilities|selectattr('severity', 'equalto', 'High')|list %}
                {% set medium = vulnerabilities|selectattr('severity', 'equalto', 'Medium')|list %}
                {% set low = vulnerabilities|selectattr('severity', 'equalto', 'Low')|list %}
                
                <div class="summary">
                    {% if critical %}
                    <div class="summary-card high">
                        <span class="number">{{ critical|length }}</span>
                        <span class="label">Critical</span>
                    </div>
                    {% endif %}
                    
                    {% if high %}
                    <div class="summary-card high">
                        <span class="number">{{ high|length }}</span>
                        <span class="label">High</span>
                    </div>
                    {% endif %}
                    
                    {% if medium %}
                    <div class="summary-card medium">
                        <span class="number">{{ medium|length }}</span>
                        <span class="label">Medium</span>
                    </div>
                    {% endif %}
                    
                    {% if low %}
                    <div class="summary-card low">
                        <span class="number">{{ low|length }}</span>
                        <span class="label">Low</span>
                    </div>
                    {% endif %}
                    
                    <div class="summary-card">
                        <span class="number">{{ vulnerabilities|length }}</span>
                        <span class="label">Total</span>
                    </div>
                </div>
            </div>
            {% endif %}
            
            <!-- Vulnerabilities Details -->
            {% if vulnerabilities %}
            <div class="section">
                <h2>🔍 Detailed Vulnerabilities</h2>
                
                <table>
                    <thead>
                        <tr>
                            <th>Vulnerability Type</th>
                            <th>Endpoint</th>
                            <th>Severity</th>
                            <th>Description</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for vuln in vulnerabilities %}
                        <tr>
                            <td><strong>{{ vuln.type }}</strong></td>
                            <td><span class="endpoint-badge">{{ vuln.endpoint }}</span></td>
                            <td>
                                <span class="severity {{ vuln.severity|lower }}">
                                    {{ vuln.severity }}
                                </span>
                            </td>
                            <td>{{ vuln.description }}</td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
            {% else %}
            <div class="section">
                <h2>🔍 Detailed Vulnerabilities</h2>
                <div class="no-data">
                    ✓ No vulnerabilities detected
                </div>
            </div>
            {% endif %}
            
            <!-- Technologies Detected -->
            {% if technologies %}
            <div class="section">
                <h2>🛠️ Technologies Detected</h2>
                
                <table>
                    <thead>
                        <tr>
                            <th>Technology</th>
                            <th>Type</th>
                            <th>Version</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for tech, details in technologies.items() %}
                        <tr>
                            <td><strong>{{ tech }}</strong></td>
                            <td>{{ details.type if details.type else 'Unknown' }}</td>
                            <td>{{ details.version if details.version else 'Unknown' }}</td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
            {% endif %}
            
            <!-- Endpoints -->
            {% if endpoints %}
            <div class="section">
                <h2>🌐 Discovered Endpoints</h2>
                <div style="display: grid; gap: 10px;">
                    {% for endpoint in endpoints %}
                    <div class="tech-tag" style="display: block; background: #e7e7ff;">
                        {{ endpoint }}
                    </div>
                    {% endfor %}
                </div>
            </div>
            {% endif %}
            
            <!-- Reconnaissance Data (if present) -->
            {% if reconnaissance %}
            <div class="section">
                <h2>📋 Reconnaissance Findings</h2>
                
                <table>
                    <thead>
                        <tr>
                            <th>Category</th>
                            <th>Key</th>
                            <th>Value</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for category, details in reconnaissance.items() %}
                            {% if details is mapping %}
                                {% for key, value in details.items() %}
                                <tr>
                                    <td><strong>{{ category }}</strong></td>
                                    <td>{{ key }}</td>
                                    <td>{{ value }}</td>
                                </tr>
                                {% endfor %}
                            {% else %}
                                <tr>
                                    <td><strong>{{ category }}</strong></td>
                                    <td>-</td>
                                    <td>{{ details }}</td>
                                </tr>
                            {% endif %}
                        {% endfor %}
                    </tbody>
                </table>
            </div>
            {% endif %}
        </div>
        
        <!-- Footer -->
        <div class="footer">
            <p><strong>VulnRadar Security Report</strong></p>
            <p>This report was automatically generated by VulnRadar vulnerability scanner</p>
            <p style="margin-top: 10px; color: #999; font-size: 0.85em;">Report generated on {{ scan_time }}</p>
        </div>
    </div>
</body>
</html>
